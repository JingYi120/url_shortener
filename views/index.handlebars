<form action="/" method="POST">
  <div class="container">
    <input type="url" name="url" placeholder="Input URL here" required />
    <button class="shorten" type="submit">Shorten</button>
  </div>
</form>
{{#if shortUrl}}
<div class="result">
  <h3>Success! Please use this link:</h3>
  <p id="generatedURL">{{shortUrl}}</p>
</div>
<button class="copy" onclick="copy()">Copy</button>

<script>
  function copy() {
    const URL = document.getElementById("generatedURL")
    navigator.clipboard.writeText(URL.innerText)
      .then(() => alert('copied'))
      .catch(error => console.log(error))
  }
</script>
{{/if}}







{{!-- <div class="container bg-white "> 
  <div class="row align-items-center">
    <div class="col mx-auto">
      <form action="/" method="POST">
        <label for="url" class="text-danger">*</label>
        <input type="text"name="url" id="url" placeholder="Please enter URL!" value="{{ longUrl }}" required>
        <button class="btn btn-info text-white" type="submit">Enter</button>

        {{#if shortUrl}}
        <div class="alert alert-info" role="alert">
          <span class="mr-2">Short URL:</span> <a href="{{shortUrl}}">
            {{shortUrl}}
          </a>
        </div>
        <button class="copy" onclick="copy()">Copy</button>
        
        <script>
          function copy() {
            const URL = document.getElementById("generatedURL")
            navigator.clipboard.writeText(URL.innerText)
              .then(() => alert('copied'))
              .catch(error => console.log(error))
          }
        </script>
        {{/if}}
      </form>
    </div>
  </div>
</div> --}}